# LINE Bot 功能與指令說明

## 📱 LINE Bot ID
**@757cbqbh**

---

## 🎯 基本功能

### 1. **自然語言記帳** ⭐
直接輸入記帳訊息，Bot 會自動解析並記錄。

**範例：**
- `午餐 150 元`
- `早餐 100`
- `交通 50`
- `收入 5000`
- `薪水 30000`

**功能特點：**
- ✅ 使用 AI 自動識別金額、類別、描述
- ✅ 自動判斷收入或支出
- ✅ 記帳後會自動餵食電子雞 🐣
- ✅ 電子雞進化時會通知 ✨
- ✅ 自動檢查預算超支並提醒 ⚠️
- ✅ 自動檢查儲蓄目標達成並通知 🎉

---

## 📋 基本指令

> 💡 **提示**：所有指令都支援自然語言輸入！您可以直接輸入中文或英文關鍵字，無需輸入 `/` 符號。例如：直接輸入 `最近記錄` 等同於 `/list`，輸入 `摘要` 等同於 `/summary`。

### `/list` 或 `/查詢` 或 `/ls` - 查詢記帳記錄

**語法：**
```
/list [數量]
最近紀錄 [數量]
最近記錄 [數量]
recent [數量]
```

**說明：**
- 查詢最近的記帳記錄（預設 10 筆，最多 50 筆）
- 會分別顯示收入和支出，使用 `i1, i2...`（收入）和 `o1, o2...`（支出）編號
- **支援自然語言**：可直接輸入中文或英文關鍵字，無需輸入 `/`

**範例：**
- `/list` 或 `最近紀錄` 或 `recent` - 查詢最近 10 筆
- `/list 20` 或 `最近紀錄 20` - 查詢最近 20 筆

**支援的自然語言關鍵字：**
- 中文：`最近紀錄`、`最近記錄`、`最近記帳`、`查詢記錄`、`查詢紀錄`、`查看記錄`、`查看紀錄`、`記錄列表`、`紀錄列表`、`查詢`、`列表`
- 英文：`recent`、`records`、`list`、`history`

**顯示格式：**
```
📝 最近的 10 筆記錄：

💰 收入：
i1. 5000 元
   類別：其他 | 薪水
   日期：2025/12/5

💸 支出：
o1. 150 元
   類別：餐飲 | 午餐
   日期：2025/12/5

💡 使用 /delete [編號] 來刪除記錄（例如：/delete i1 或 /delete o1）
```

---

### `/summary` 或 `/摘要` 或 `/sum` - 查看記帳摘要

**語法：**
```
/summary
摘要
summary
```

**說明：**
- 顯示總收入、總支出和餘額
- **支援自然語言**：可直接輸入中文或英文關鍵字，無需輸入 `/`

**支援的自然語言關鍵字：**
- 中文：`摘要`、`總結`、`總覽`、`統計`、`總計`
- 英文：`summary`、`overview`、`statistics`、`stats`

**範例輸出：**
```
📊 記帳摘要：

總收入：50000 元
總支出：15000 元
餘額：35000 元
```

---

### `/delete` 或 `/刪除` 或 `/del` - 刪除記帳記錄

**語法：**
```
/delete [編號或ID]
刪除 [編號或ID]
delete [編號或ID]
```

**說明：**
- 支援多種格式：
  - 編號格式：`/delete i1`（刪除第一筆收入）、`/delete o2`（刪除第二筆支出）
  - 數字格式：`/delete 1`（刪除第一筆記錄）
  - ID 格式：`/delete [交易ID]`
- **支援自然語言**：可直接輸入中文或英文關鍵字，無需輸入 `/`

**範例：**
- `/delete i1` 或 `刪除 i1` 或 `delete i1` - 刪除第一筆收入記錄
- `/delete o2` 或 `刪掉 o2` 或 `remove o2` - 刪除第二筆支出記錄
- `/delete 1` 或 `刪除 1` - 刪除第一筆記錄（舊格式，仍支援）

**支援的自然語言關鍵字：**
- 中文：`刪除`、`刪掉`、`移除`
- 英文：`delete`、`remove`、`del`

**注意：**
- 建議先使用 `/list` 查看記錄編號
- 編號會自動更新，以最新查詢結果為準

---

### `/pet` 或 `/寵物` 或 `/電子雞` - 查看電子雞狀態

**語法：**
```
/pet
電子雞
寵物
pet
```

**說明：**
- 查看您的電子雞（Tamagotchi）的詳細狀態
- 包括等級、階段、飽食度、心情值、健康度等
- **支援自然語言**：可直接輸入中文或英文關鍵字，無需輸入 `/`

**支援的自然語言關鍵字：**
- 中文：`電子雞`、`寵物`、`我的寵物`、`寵物狀態`、`我的雞`、`小雞`
- 英文：`pet`、`tamagotchi`

**顯示內容：**
- 🐣 階段：蛋 / 嬰兒期 / 兒童期 / 成年期 / 生病 / 垂死 / 死亡
- 📊 等級、飽食度、心情值、健康度
- 📈 連續記帳天數、總記帳筆數
- 💬 當前狀態訊息

**電子雞狀態說明：**
- 記帳時會自動餵食電子雞
- 進化時會收到通知
- 健康度會根據預算使用情況變化

---

### `/myid` 或 `/id` - 獲取 LINE 用戶 ID

**語法：**
```
/myid
我的id
myid
```

**說明：**
- 獲取您的 LINE 用戶 ID
- 用於在網頁版連結 Google 帳號，實現 LINE Bot 和網頁版的資料同步
- **支援自然語言**：可直接輸入中文或英文關鍵字，無需輸入 `/`

**支援的自然語言關鍵字：**
- 中文：`我的id`、`用戶id`、`我的用戶id`
- 英文：`myid`、`id`、`userid`、`line id`

**使用流程：**
1. 在 LINE Bot 輸入 `/myid`
2. 複製顯示的 LINE 用戶 ID
3. 在網頁版（https://final-lac-alpha.vercel.app）登入 Google 帳號
4. 在側邊欄底部輸入 LINE 用戶 ID 並連結

---

### `/savings` 或 `/goal` 或 `/儲蓄目標` - 查看儲蓄目標

**語法：**
```
/savings
/goal
儲蓄目標
查看儲蓄
```

**說明：**
- 查看所有儲蓄目標的進度和狀態
- 顯示目標名稱、目標金額、當前進度、達成百分比、剩餘金額、期限等
- **支援自然語言**：可直接輸入中文或英文關鍵字，無需輸入 `/`

**支援的自然語言關鍵字：**
- 中文：`儲蓄`、`儲蓄目標`、`目標`、`我的目標`、`查看目標`、`查看儲蓄`
- 英文：`savings`、`goal`

**範例輸出：**
```
💰 儲蓄目標總覽：

🎯 旅遊
目標：50,000 元
目前：25,000 元（50.0%）
還需：25,000 元
期限：2025/12/31（剩餘 250 天）

🎯 買車
目標：300,000 元
目前：50,000 元（16.7%）
還需：250,000 元
```

**如果沒有設定目標：**
```
💰 目前沒有設定儲蓄目標

💡 設定方式：
「儲蓄目標 [名稱] [金額]」
例如：儲蓄目標 旅遊 50000

或使用：
「設定儲蓄目標 [名稱] [金額]」
```

---

### 設定儲蓄目標

**語法：**
```
儲蓄目標 [名稱] [金額]
設定儲蓄目標 [名稱] [金額]
儲蓄目標 [名稱] [金額] [期限]
```

**說明：**
- 使用自然語言設定儲蓄目標（不需要斜線指令）
- 名稱：目標的名稱（例如：旅遊、買車、買房）
- 金額：目標金額（數字，單位：元）
- 期限（可選）：目標達成日期，格式：`YYYY-MM-DD` 或 `YYYY/MM/DD`

**範例：**
- `儲蓄目標 旅遊 50000` - 設定旅遊目標 50,000 元（無期限）
- `設定儲蓄目標 買車 300000` - 設定買車目標 300,000 元
- `儲蓄目標 買房 5000000 2025-12-31` - 設定買房目標 5,000,000 元，期限 2025/12/31

**範例輸出：**
```
✅ 儲蓄目標設定成功！

目標名稱：旅遊
目標金額：50,000 元
當前進度：25,000 元（50.0%）
還需：25,000 元
期限：2025/12/31（剩餘 250 天）
```

**儲蓄計算說明：**
- 儲蓄金額 = 總收入 - 總支出（淨儲蓄）
- 每次記帳後會自動更新所有儲蓄目標的進度
- 當儲蓄金額達到目標時，會自動收到 LINE 通知 🎉

---

### `/help` 或 `/幫助` 或 `/h` - 顯示使用說明

**語法：**
```
/help
幫助
help
```

**說明：**
- 顯示所有可用指令和使用說明
- 包含網頁版連結資訊
- **支援自然語言**：可直接輸入中文或英文關鍵字，無需輸入 `/`

**支援的自然語言關鍵字：**
- 中文：`幫助`、`說明`、`使用說明`、`如何使用`、`功能`、`說明書`、`教學`
- 英文：`help`

---

## 👥 群組分帳功能

> ⚠️ **注意：此功能僅在 LINE 群組中使用，不支援私聊**

### 功能概述
群組分帳功能可以幫助群組成員：
- 記錄群組共同支出（例如：聚餐、旅遊）
- 計算每個人應該分攤的金額
- 自動計算誰應該給誰錢
- 結算後自動匯入個人記帳（顯示為「xx群組出資」和「xx群組回收」）

---

### `/group new` 或 `/group 新建` - 建立新分帳

**語法：**
```
/group new [總金額] [描述]
```

**說明：**
- 在群組中建立一個新的分帳記錄
- 總金額為群組共同支出的總額

**範例：**
- `/group new 1000 晚餐`
- `/group new 5000 旅遊`

**輸出：**
```
✅ 已建立群組分帳
總金額：1000 元
描述：晚餐

💡 使用 /group add [金額] 添加您的出資
使用 /group split [金額] 設定您的分攤
```

---

### `/group add` 或 `/group 出資` - 記錄您的出資金額

**語法：**
```
/group add [金額]
```

**說明：**
- 記錄您實際為這次群組支出支付的金額
- 可以多人分別記錄自己的出資金額

**範例：**
- `/group add 300` - 表示您實際出了 300 元

**輸出：**
```
✅ 已記錄您的出資：300 元
💡 使用 /group split [金額] 設定您的分攤金額
```

---

### `/group split` 或 `/group 分攤` - 設定您的分攤金額

**語法：**
```
/group split [金額]
```

**說明：**
- 設定您應該分攤的金額
- 這個金額通常是總金額除以人數，或根據您的消費計算

**範例：**
- `/group split 250` - 表示您應分攤 250 元

**輸出：**
```
✅ 已設定您的分攤：250 元
```

**重要：**
- 必須先使用 `/group add` 記錄出資
- 如果出資 > 分攤，表示您應該收到錢
- 如果出資 < 分攤，表示您應該付錢

---

### `/group list` 或 `/group 查看` - 查看分帳狀態

**語法：**
```
/group list
```

**說明：**
- 查看當前分帳的詳細狀態
- 顯示所有參與者的出資和分攤情況
- 自動計算並顯示轉帳建議（誰應該給誰錢）

**輸出範例：**
```
📋 當前分帳狀態：

總金額：1000 元
描述：晚餐

參與者：
1. 出資：300 元，分攤：250 元（應收 50 元）
2. 出資：200 元，分攤：250 元（應付 50 元）
3. 出資：500 元，分攤：500 元（已平衡）

💰 轉帳建議：
- 用戶2 應該給 用戶1 50 元

💡 使用 /group settle 結算並匯入個人記帳
```

---

### `/group settle` 或 `/group 結算` - 結算並匯入個人記帳

**語法：**
```
/group settle
```

**說明：**
- 結算當前分帳並自動匯入所有參與者的個人記帳
- 會為每個參與者創建：
  - **「xx群組出資」** 記錄（支出）- 記錄您的實際出資金額
  - **「xx群組回收」** 記錄（收入）- 記錄您應該收到的金額
  - 如果出資 < 分攤，會創建對應的支出記錄

**前提條件：**
- 所有參與者都必須設定分攤金額（使用 `/group split`）

**輸出範例：**
```
✅ 分帳已結算並匯入個人記帳

💰 轉帳建議：
- 用戶2 應該給 用戶1 50 元

💡 已為所有參與者創建「群組出資」和「群組回收」記錄
```

**結算後的個人記帳記錄：**
- 如果出資 300 元，分攤 250 元：
  - 支出：「晚餐群組出資」300 元
  - 收入：「晚餐群組回收」250 元
  - 實際：應收 50 元（在網頁版可以看到明細）

---

### `/group help` 或 `/group 幫助` - 查看詳細說明

**語法：**
```
/group help
```

**說明：**
- 顯示群組分帳功能的詳細使用說明

---

## 🔗 與網頁版同步

### 連結流程：
1. 在 LINE Bot 輸入 `/myid` 獲取您的 LINE 用戶 ID
2. 訪問網頁版：https://final-lac-alpha.vercel.app
3. 使用 Google 帳號登入
4. 在側邊欄底部輸入 LINE 用戶 ID 並點擊「連接」
5. 完成後，LINE Bot 的記帳記錄會自動同步到網頁版

### 同步內容：
- ✅ 所有記帳記錄（收入和支出）
- ✅ 電子雞狀態和進度
- ✅ 群組分帳結算記錄

---

## 📊 功能總覽表

| 功能 | 斜線指令 | 自然語言範例 | 支援位置 | 說明 |
|------|----------|--------------|----------|------|
| **自然語言記帳** | - | 直接輸入訊息 | 私聊/群組 | 自動解析並記錄 |
| **查詢記錄** | `/list` | `最近記錄`、`recent` | 私聊/群組 | 查看最近的記帳記錄 |
| **查看摘要** | `/summary` | `摘要`、`summary` | 私聊/群組 | 總收入、總支出、餘額 |
| **刪除記錄** | `/delete` | `刪除 i1`、`delete o2` | 私聊/群組 | 刪除指定記錄 |
| **電子雞狀態** | `/pet` | `電子雞`、`pet` | 私聊/群組 | 查看寵物狀態 |
| **查看儲蓄目標** | `/savings` 或 `/goal` | `儲蓄目標`、`目標`、`查看儲蓄` | 私聊/群組 | 查看儲蓄目標進度 |
| **設定儲蓄目標** | (自然語言) | `儲蓄目標 旅遊 50000` | 私聊/群組 | 設定儲蓄目標（支援期限） |
| **獲取用戶ID** | `/myid` | `我的id`、`myid` | 私聊/群組 | 用於連結網頁版 |
| **設定預算** | (Quick Reply) | `單日預算：1000` | 私聊/群組 | 設定日/週/月預算上限 |
| **使用說明** | `/help` | `幫助`、`help` | 私聊/群組 | 顯示所有指令 |
| **建立分帳** | `/group new` | **僅群組** | 建立群組分帳 |
| **記錄出資** | `/group add` | **僅群組** | 記錄實際出資金額 |
| **設定分攤** | `/group split` | **僅群組** | 設定應分攤金額 |
| **查看分帳** | `/group list` | **僅群組** | 查看分帳狀態 |
| **結算分帳** | `/group settle` | **僅群組** | 結算並匯入個人記帳 |
| **分帳說明** | `/group help` | **僅群組** | 群組分帳詳細說明 |

---

## 💡 使用技巧

1. **快速記帳**：直接輸入「午餐 150」，無需輸入指令
2. **查看分類**：記錄會自動分類（餐飲、交通、購物等）
3. **電子雞互動**：每次記帳都會餵食電子雞，保持記帳習慣可以讓它進化
4. **儲蓄目標**：設定儲蓄目標後，達成時會自動收到通知 🎉
5. **預算提醒**：設定預算後，超支時會自動提醒 ⚠️
6. **群組分帳**：適合聚餐、旅遊等共同支出場景
7. **資料同步**：連結 Google 帳號後，可在網頁版查看更詳細的統計圖表

---

## ❓ 常見問題

**Q: 為什麼我的訊息無法被解析？**
A: 請確保訊息包含金額（例如：150、150元），Bot 會自動識別類別和描述。

**Q: 群組分帳功能在哪裡使用？**
A: 必須在 LINE 群組中使用，私聊不支援。

**Q: 如何取消連結網頁版？**
A: 在網頁版側邊欄底部點擊「解除連結」按鈕。

**Q: 電子雞會死亡嗎？**
A: 如果長期不記帳或超出預算，電子雞可能會生病或死亡。保持記帳習慣可以維持健康。

**Q: 儲蓄目標如何計算？**
A: 儲蓄金額 = 總收入 - 總支出（淨儲蓄）。每次記帳後會自動更新目標進度。

**Q: 儲蓄目標達成後會怎樣？**
A: 當儲蓄金額達到目標時，會自動收到 LINE 通知，告知目標已達成。

---

**最後更新：2025-12-19**

